apiVersion: v1
kind: Service
metadata:
  name: {{ include "tv-machine-translation-service.fullname" . }}
  labels:
    {{- include "tv-machine-translation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tv-machine-translation-service.selectorLabels" . | nindent 4 }}
