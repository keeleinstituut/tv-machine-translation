apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tv-machine-translation-service.fullname" . }}-test-connection"
  labels:
    {{- include "tv-machine-translation-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tv-machine-translation-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
